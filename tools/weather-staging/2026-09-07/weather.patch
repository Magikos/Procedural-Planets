--- a/Assets/Graphics/Shaders/Includes/WeatherSampling.hlsl
+++ b/Assets/Graphics/Shaders/Includes/WeatherSampling.hlsl
@@ -11,11 +11,19 @@
 SAMPLER(sampler_WeatherDynamicsMap);
 float4x4 _CloudWeatherRotation;
 float4 _PrecipitationParams;
+float4 _WeatherParticlePhaseParams;
 float _WeatherCloudTypeTest;
 
 #include "WeatherCubeFace.hlsl"
 #include "WeatherLightning.hlsl"
 #include "WeatherThreshold.hlsl"
+
+float WeatherSnowFraction(float temperatureCelsius)
+{
+    float blend = max(_WeatherParticlePhaseParams.y, 0.1);
+    return 1.0 - smoothstep(_WeatherParticlePhaseParams.x - blend,
+        _WeatherParticlePhaseParams.x + blend, temperatureCelsius);
+}
 
 float WeatherCloudConvectivity(float3 direction, float temperature01)
 {
--- a/Assets/Graphics/Shaders/RainParticles.shader
+++ b/Assets/Graphics/Shaders/RainParticles.shader
@@ -23,6 +23,7 @@
 
             #include "Packages/com.unity.render-pipelines.universal/ShaderLibrary/Core.hlsl"
             #include "Includes/WeatherSampling.hlsl"
+            #include "Includes/ClimateSampling.hlsl"
 
             // Must match the layout of `Raindrop` in RainParticleUpdate.compute.
             struct Raindrop
@@ -43,6 +44,7 @@
             float4 _RainColor;
             float  _RainVisibilityThreshold;   // dynamics.b threshold below which drops are fully invisible.
             float  _RainDensityScale;          // multiplier on dynamics.b for the density gate. Higher = more drops visible at the same rain rate; saturates at 1.
+            float  _RainNearRadius;
             float3 _PlanetCenter;
             float  _SeaRadius;
             float4 _WeatherLightningColor;
@@ -102,7 +104,13 @@
                 float speed = max(length(r.Velocity), 0.001);
                 float3 motionDir = r.Velocity / speed;
                 float3 viewDir = normalize(_WorldSpaceCameraPos.xyz - r.Position);
-                float3 sideDir = normalize(cross(viewDir, motionDir));
+                float3 sideDir = cross(viewDir, motionDir);
+                if (dot(sideDir, sideDir) < 0.000001)
+                {
+                    float3 reference = abs(motionDir.y) < 0.9 ? float3(0, 1, 0) : float3(1, 0, 0);
+                    sideDir = cross(reference, motionDir);
+                }
+                sideDir = normalize(sideDir);
 
                 float side;
                 float along;
@@ -122,11 +130,10 @@
 
                 // Drops only render where raw rain overlaps a storm cell.
                 float3 normal = normalize(r.Position - _PlanetCenter);
-                float4 dynamics = SampleDynamics(normal);
                 float storm = saturate(SampleWeather(normal).g);
-                float stormGate = smoothstep(_PrecipitationParams.y,
-                    min(1.0, _PrecipitationParams.y + _PrecipitationParams.z), storm);
-                float rainSignal = saturate(dynamics.b) * stormGate;
+                float temperature = ClimateTemperatureCelsius(SampleClimate01(normal).x);
+                float rainSignal = WeatherPrecipitationSignal(normal, storm)
+                    * (1.0 - WeatherSnowFraction(temperature));
                 o.lightning = WeatherLightning(normal, storm) * _WeatherLightningColor.a;
 
                 // Per-particle DENSITY gate. Each particle has a stable per-id
@@ -147,7 +154,10 @@
                     rainSignal);
                 float visible = (dropRank < densityFactor) ? 1.0 : 0.0;
 
-                o.alpha = visible * thresholdFade * _RainColor.a;
+                float distanceFade = 1.0 - smoothstep(_RainNearRadius * 0.8, _RainNearRadius,
+                    distance(r.Position, _WorldSpaceCameraPos.xyz));
+                o.alpha = visible * thresholdFade * _RainColor.a * distanceFade
+                    * saturate(r.Life01);
                 return o;
             }
 
--- a/Assets/Graphics/Shaders/WeatherEvolution.compute
+++ b/Assets/Graphics/Shaders/WeatherEvolution.compute
@@ -17,6 +17,8 @@
 Texture2DArray<float4> _WeatherRead;
 RWTexture2DArray<float4> _WeatherWrite;
 Texture2DArray<float4> _DynamicsRead;
+Texture2DArray<float2> _ClimateRead;
+int _HasClimate;
 RWTexture2DArray<float4> _DynamicsWrite;
 
 int _Resolution;
@@ -152,12 +154,14 @@
     float storm = saturate(advectedWeather.g);
     float humidity = saturate(advectedDynamics.r);
     float precipitationWater = saturate(advectedDynamics.g);
-    float moistureSupply = saturate(localDynamics.a);
-
-    // The stationary channel is a front potential map, not raw humidity. Evolving
-    // toward it keeps the authored cloud structure stable while still allowing
-    // cells to condense and dry over time.
-    float targetCondensation = saturate(source);
+    float moistureSupply = _HasClimate != 0
+        ? saturate(_ClimateRead.SampleLevel(sampler_linear_clamp, float3(uv, id.z), 0).g)
+        : saturate(localDynamics.a);
+
+    // Use the same moisture cost for cloud support and for growth consumption.
+    // A seeded front cannot sustain cloud after its available humidity dries out.
+    const float condensationHumidityCost = 0.18;
+    float targetCondensation = min(saturate(source), humidity / condensationHumidityCost);
     float condensationDeltaToTarget = targetCondensation - condensation;
     float growthRate = _MoistureSourceStrength * lerp(0.45, 1.0, targetCondensation);
     float dryingRate = _DryAirEvaporationRate * lerp(1.2, 0.55, targetCondensation);
@@ -184,7 +188,7 @@
     nextPrecipitationWater = saturate(nextPrecipitationWater - rainOut);
 
     float humidityRecovered = MoveTowards(humidity, moistureSupply, _HumidityRecoveryRate * _DeltaTime);
-    float humidityConsumed = max(nextCondensation - condensation, 0.0) * 0.18 + rainOut * 0.35;
+    float humidityConsumed = max(nextCondensation - condensation, 0.0) * condensationHumidityCost + rainOut * 0.35;
     float nextHumidity = saturate(humidityRecovered - humidityConsumed);
 
     float condensationDelta = nextCondensation - condensation;
--- a/Assets/Graphics/Shaders/WeatherParticles.shader
+++ b/Assets/Graphics/Shaders/WeatherParticles.shader
@@ -20,7 +20,6 @@
 float4 _WeatherParticleCounts;
 float4 _WeatherParticleDustParams;
 float4 _WeatherParticleSnowParams;
-float4 _WeatherParticlePhaseParams;
 float4 _WeatherParticleDustColor;
 float4 _WeatherParticleSnowColor;
 float4 _PrecipitationVisualParams;
@@ -166,11 +165,7 @@
     float stormGate = WeatherThreshold(_PrecipitationParams.y,
         min(1.0, _PrecipitationParams.y + _PrecipitationParams.z), storm);
     float rainSignal = saturate(dynamics.b) * stormGate;
-    float snowBlend = max(_WeatherParticlePhaseParams.y, 0.1);
-    float snowPhase = 1.0 - smoothstep(
-        _WeatherParticlePhaseParams.x - snowBlend,
-        _WeatherParticlePhaseParams.x + snowBlend,
-        temperatureCelsius);
+    float snowPhase = WeatherSnowFraction(temperatureCelsius);
 
     float proofVisibility = ProfileProofVisibility(profile);
     float visibility;
--- a/Assets/Resources/RainParticleUpdate.compute
+++ b/Assets/Resources/RainParticleUpdate.compute
@@ -35,6 +35,7 @@
 float   _DeltaTime;
 uint    _FrameSeed;            // perturbed per frame so respawn locations vary over time.
 uint    _ActiveCount;          // live particle count; dispatch rounds up to 64, so guard the pad lanes.
+uint    _ResetParticles;
 
 uint HashUint(uint x)
 {
@@ -99,15 +100,27 @@
     float spawnAltitudeT = randomAltitude
         ? Hash01(seed * 0xc2b2ae35u)
         : (0.88 + Hash01(seed * 0xface5u) * 0.12);
-    float spawnRadius = lerp(_SeaRadius, _CloudBottomRadius, spawnAltitudeT);
+    float cameraRadius = length(_CameraPosition - _PlanetCenter);
+    float spawnBottom = max(_SeaRadius, cameraRadius - _CameraNearRadius);
+    float spawnTop = max(spawnBottom, min(_CloudBottomRadius, cameraRadius + _CameraNearRadius));
+    float spawnRadius = lerp(spawnBottom, spawnTop, spawnAltitudeT);
 
     // Offset the spawn position toward the camera. We want the particle within
     // the camera's near radius. Pick a horizontal distance proportional to the
     // near radius so the spawn distribution covers the visible disc.
-    float spawnHorizDist = Hash01(seed * 0xdeadbeefu) * _CameraNearRadius;
+    float verticalOffset = spawnRadius - cameraRadius;
+    float horizontalRadius = sqrt(max(0.0, _CameraNearRadius * _CameraNearRadius
+        - verticalOffset * verticalOffset));
+    float spawnHorizDist = sqrt(Hash01(seed * 0xdeadbeefu)) * horizontalRadius;
     float angularOffset = spawnHorizDist / max(_SeaRadius, 1.0);
     float3 cameraDir = normalize(_CameraPosition - _PlanetCenter);
-    float3 offsetDir = normalize(direction - cameraDir * dot(direction, cameraDir));
+    float3 offsetDir = direction - cameraDir * dot(direction, cameraDir);
+    if (dot(offsetDir, offsetDir) < 0.000001)
+    {
+        float3 reference = abs(cameraDir.y) < 0.9 ? float3(0, 1, 0) : float3(1, 0, 0);
+        offsetDir = cross(reference, cameraDir);
+    }
+    offsetDir = normalize(offsetDir);
     float3 spawnNormal = normalize(cameraDir * cos(angularOffset) + offsetDir * sin(angularOffset));
 
     r.Position = _PlanetCenter + spawnNormal * spawnRadius;
@@ -117,8 +130,10 @@
     // exact speed evolves with the particle's slot id consistently.
     float3 downAtSpawn = normalize(_PlanetCenter - r.Position);
     float speedJitter = lerp(0.80, 1.20, Hash01(id * 0xfeedbeefu));
-    r.Velocity = downAtSpawn * _FallSpeedMps * speedJitter;
-    r.Life01 = 1.0;
+    float3 windHorizontal = _WindDirection - downAtSpawn * dot(_WindDirection, downAtSpawn);
+    r.Velocity = downAtSpawn * _FallSpeedMps * speedJitter
+        + windHorizontal * _WindSpeedMps * _WindCoupling;
+    r.Life01 = 0.001;
 }
 
 [numthreads(64, 1, 1)]
@@ -129,7 +144,7 @@
 
     Raindrop r = _RainParticles[id.x];
 
-    bool needsInitialSpawn = r.Life01 <= 0.0001;
+    bool needsInitialSpawn = r.Life01 <= 0.0001 || _ResetParticles != 0;
     if (needsInitialSpawn)
     {
         // First-time spawn: random altitude so the system looks already-running.
@@ -160,6 +175,7 @@
                + windHorizontal * _WindSpeedMps * _WindCoupling;
 
     r.Position += r.Velocity * _DeltaTime;
+    r.Life01 = saturate(r.Life01 + _DeltaTime * 10.0);
 
     // Respawn checks.
     float currentDist = length(r.Position - _PlanetCenter);
--- /dev/null
+++ b/Assets/Resources/Weather/Thunder/Thunder-4.wav.meta
@@ -0,0 +1,29 @@
+fileFormatVersion: 2
+guid: 5959182eaa7f4e0992ee635e9b54aee2
+timeCreated: 1523656442
+licenseType: Store
+AudioImporter:
+  serializedVersion: 6
+  defaultSettings:
+    loadType: 0
+    sampleRateSetting: 0
+    sampleRateOverride: 44100
+    compressionFormat: 1
+    quality: 1
+    conversionMode: 0
+  platformSettingOverrides: {}
+  forceToMono: 1
+  normalize: 1
+  preloadAudioData: 1
+  loadInBackground: 0
+  3D: 1
+  userData: 
+  assetBundleName: 
+  assetBundleVariant: 
+AssetOrigin:
+  serializedVersion: 1
+  productId: 2714
+  packageName: UniStorm - Volumetric Clouds, Sky, Modular Weather, and Cloud Shadows
+  packageVersion: 5.4.1
+  assetPath: Assets/UniStorm Weather System/Sounds/Thunder/Thunder 4.wav
+  uploadId: 922632
--- /dev/null
+++ b/Assets/Resources/Weather/Thunder/Thunder-5.wav.meta
@@ -0,0 +1,29 @@
+fileFormatVersion: 2
+guid: 51d7515271db408597c4e743ce88f228
+timeCreated: 1523656442
+licenseType: Store
+AudioImporter:
+  serializedVersion: 6
+  defaultSettings:
+    loadType: 0
+    sampleRateSetting: 0
+    sampleRateOverride: 44100
+    compressionFormat: 1
+    quality: 1
+    conversionMode: 0
+  platformSettingOverrides: {}
+  forceToMono: 1
+  normalize: 1
+  preloadAudioData: 1
+  loadInBackground: 0
+  3D: 1
+  userData: 
+  assetBundleName: 
+  assetBundleVariant: 
+AssetOrigin:
+  serializedVersion: 1
+  productId: 2714
+  packageName: UniStorm - Volumetric Clouds, Sky, Modular Weather, and Cloud Shadows
+  packageVersion: 5.4.1
+  assetPath: Assets/UniStorm Weather System/Sounds/Thunder/Thunder 5.wav
+  uploadId: 922632
--- /dev/null
+++ b/Assets/Resources/Weather/Thunder/Thunder-6.wav.meta
@@ -0,0 +1,29 @@
+fileFormatVersion: 2
+guid: 995fa72a737b4b318d0829a6ae85144f
+timeCreated: 1523656441
+licenseType: Store
+AudioImporter:
+  serializedVersion: 6
+  defaultSettings:
+    loadType: 0
+    sampleRateSetting: 0
+    sampleRateOverride: 44100
+    compressionFormat: 1
+    quality: 1
+    conversionMode: 0
+  platformSettingOverrides: {}
+  forceToMono: 1
+  normalize: 1
+  preloadAudioData: 1
+  loadInBackground: 0
+  3D: 1
+  userData: 
+  assetBundleName: 
+  assetBundleVariant: 
+AssetOrigin:
+  serializedVersion: 1
+  productId: 2714
+  packageName: UniStorm - Volumetric Clouds, Sky, Modular Weather, and Cloud Shadows
+  packageVersion: 5.4.1
+  assetPath: Assets/UniStorm Weather System/Sounds/Thunder/Thunder 6.wav
+  uploadId: 922632
--- a/Assets/Scripts/Planet/Clouds/SphericalWeatherGrid.cs
+++ b/Assets/Scripts/Planet/Clouds/SphericalWeatherGrid.cs
@@ -87,6 +87,10 @@
     static readonly int _weatherReadId = Shader.PropertyToID("_WeatherRead");
     static readonly int _weatherWriteId = Shader.PropertyToID("_WeatherWrite");
     static readonly int _dynamicsReadId = Shader.PropertyToID("_DynamicsRead");
+    static readonly int _climateReadId = Shader.PropertyToID("_ClimateRead");
+    static readonly int _hasClimateId = Shader.PropertyToID("_HasClimate");
+    static readonly int _climateMapId = Shader.PropertyToID(ShaderGlobalIds.ClimateMap);
+    static readonly int _climateResolutionId = Shader.PropertyToID(ShaderGlobalIds.ClimateMapResolution);
     static readonly int _dynamicsWriteId = Shader.PropertyToID("_DynamicsWrite");
     static readonly int _resolutionId = Shader.PropertyToID("_Resolution");
     static readonly int _permutationsId = Shader.PropertyToID("_Permutations");
@@ -435,6 +439,10 @@
         compute.SetTexture(kernel, _weatherWriteId, _scratchTexture);
         compute.SetTexture(kernel, _dynamicsReadId, _dynamicsActiveTexture);
         compute.SetTexture(kernel, _dynamicsWriteId, _dynamicsScratchTexture);
+        Texture climate = Shader.GetGlobalTexture(_climateMapId);
+        bool hasClimate = climate != null && Shader.GetGlobalFloat(_climateResolutionId) > 0f;
+        compute.SetTexture(kernel, _climateReadId, hasClimate ? climate : _dynamicsActiveTexture);
+        compute.SetInt(_hasClimateId, hasClimate ? 1 : 0);
         compute.SetInt(_resolutionId, Resolution);
         compute.SetFloat(_deltaTimeId, deltaTime);
         compute.SetFloat(_stormThresholdId, settings.StormThreshold);
--- a/Assets/Scripts/Planet/Precipitation/RainParticleController.cs
+++ b/Assets/Scripts/Planet/Precipitation/RainParticleController.cs
@@ -67,7 +67,8 @@
     [Tooltip("Drop color. Brighter (closer to white) cuts through atmospheric scattering tint better than a gray-blue.")]
     public Color RainColor = new Color(0.92f, 0.95f, 1.0f, 0.95f);
 
-    public bool IsReadyToDraw => _ready && _particleBuffer != null && _runtimeMaterial != null;
+    public bool IsReadyToDraw => isActiveAndEnabled && _presenting && _ready
+        && _particleBuffer != null && _runtimeMaterial != null;
     public int ParticleCount => _ready ? Mathf.Min(ParticleCountSetting, _allocatedCount) : 0;
     public Material Material => _runtimeMaterial;
 
@@ -79,6 +80,8 @@
     int _updateKernel;
     int _allocatedCount;
     bool _ready;
+    bool _presenting;
+    bool _resetParticles = true;
 
     Vector3 _planetCenter;
     float _seaLevelRadius;
@@ -86,6 +89,9 @@
     float _cloudTopRadius;
     IWeatherProvider _weatherProvider;
     IPrecipitationDebugControl _precipControl;
+    IWaterQueryService _waterQuery;
+    CloudDto _cloudSettings;
+    PrecipitationDto _precipSettings;
     float _altitudeFadeAlpha = 1f;
 
     // --- Compute shader property IDs -------------------------------------------------
@@ -105,6 +111,7 @@
     static readonly int _deltaTimeId = Shader.PropertyToID("_DeltaTime");
     static readonly int _frameSeedId = Shader.PropertyToID("_FrameSeed");
     static readonly int _activeCountId = Shader.PropertyToID("_ActiveCount");
+    static readonly int _resetParticlesId = Shader.PropertyToID("_ResetParticles");
 
     // --- Render material property IDs (set on shared material) -----------------------
     static readonly int _rainStreakWidthId = Shader.PropertyToID("_RainStreakWidth");
@@ -112,6 +119,7 @@
     static readonly int _rainColorId = Shader.PropertyToID("_RainColor");
     static readonly int _rainVisibilityThresholdId = Shader.PropertyToID("_RainVisibilityThreshold");
     static readonly int _rainDensityScaleId = Shader.PropertyToID("_RainDensityScale");
+    static readonly int _rainNearRadiusId = Shader.PropertyToID("_RainNearRadius");
     static readonly int _rainPlanetCenterId = Shader.PropertyToID(ShaderGlobalIds.PlanetCenter);
     static readonly int _rainSeaRadiusId = Shader.PropertyToID("_SeaRadius");
 
@@ -123,12 +131,18 @@
     void OnEnable()
     {
         EventBus<PlanetGeneratedEvent>.Listen(OnPlanetGenerated);
+        EventBus<SettingsChangedEvent>.Listen(OnSettingsChanged);
+        RefreshSettings();
+        _resetParticles = true;
         EnsureResources();
     }
 
     void OnDisable()
     {
         EventBus<PlanetGeneratedEvent>.Unlisten(OnPlanetGenerated);
+        EventBus<SettingsChangedEvent>.Unlisten(OnSettingsChanged);
+        _presenting = false;
+        _resetParticles = true;
     }
 
     void OnDestroy()
@@ -140,25 +154,29 @@
     {
         _planetCenter = evt.PlanetCenter;
         _seaLevelRadius = evt.SeaLevelRadius > 0f ? evt.SeaLevelRadius : evt.PlanetRadius;
-
-        // Same formula PrecipitationController uses for _PrecipitationRadii.y, computed
-        // from the DTOs directly: reading the shader global back raced its own
-        // PlanetGeneratedEvent handler (listener order is not guaranteed).
-        float cloudBase = 330f;
-        float bottomAltitude = 25f;
-        float cloudBaseOverlap = 45f;
-        if (SettingsProvider.IsRegistered<CloudDto>())
-            cloudBase = SettingsProvider.GetSettings<CloudDto>().BaseAltitude;
-        if (SettingsProvider.IsRegistered<PrecipitationDto>())
-        {
-            var precip = SettingsProvider.GetSettings<PrecipitationDto>();
-            bottomAltitude = precip.BottomAltitude;
-            cloudBaseOverlap = precip.CloudBaseOverlap;
-        }
-        _cloudBottomRadius = _seaLevelRadius
-            + Mathf.Max(bottomAltitude + 1f, cloudBase + cloudBaseOverlap);
+        ServiceLocator.TryGet(out _weatherProvider);
+        ServiceLocator.TryGet(out _precipControl);
+        ServiceLocator.TryGet(out _waterQuery);
+        RefreshSettings();
+        _resetParticles = true;
+        _ready = true;
+    }
+
+    void OnSettingsChanged(SettingsChangedEvent evt)
+    {
+        if (evt.DtoType == typeof(CloudDto) || evt.DtoType == typeof(PrecipitationDto))
+            RefreshSettings();
+    }
+
+    void RefreshSettings()
+    {
+        _cloudSettings = SettingsProvider.IsRegistered<CloudDto>()
+            ? SettingsProvider.GetSettings<CloudDto>() : null;
+        _precipSettings = SettingsProvider.IsRegistered<PrecipitationDto>()
+            ? SettingsProvider.GetSettings<PrecipitationDto>() : null;
+        _cloudBottomRadius = _seaLevelRadius + (_precipSettings?.ColumnTopAltitude(_cloudSettings)
+            ?? ((_cloudSettings?.BaseAltitude ?? 330f) + 45f));
         _cloudTopRadius = _cloudBottomRadius + 60f;
-        _ready = true;
     }
 
     void EnsureResources()
@@ -201,6 +219,7 @@
         var zero = new byte[desired * RaindropStride];
         _particleBuffer.SetData(zero);
         _allocatedCount = desired;
+        _resetParticles = true;
     }
 
     void ReleaseBuffer()
@@ -225,6 +244,18 @@
         if (!_ready)
             return;
 
+        Camera cam = Camera.main;
+        bool eligible = cam != null && _precipControl != null
+            && _precipControl.ShouldRenderRainParticles(cam)
+            && (_waterQuery == null || !_waterQuery.IsUnderwater(cam.transform.position))
+            && ParticleCountSetting > 0;
+        if (!eligible)
+        {
+            _presenting = false;
+            _resetParticles = true;
+            return;
+        }
+
         EnsureResources();
         if (_particleBuffer == null || _updateCompute == null)
             return;
@@ -232,25 +263,22 @@
         if (_allocatedCount != Mathf.Max(64, ParticleCountSetting))
             EnsureBuffer();
 
-        UpdateAltitudeFade();
+        UpdateAltitudeFade(cam);
         UploadMaterialParams();
-        DispatchUpdate();
-    }
-
-    void UpdateAltitudeFade()
-    {
-        if (_precipControl == null)
-            ServiceLocator.TryGet(out _precipControl);
-
-        Camera cam = Camera.main;
-        if (_precipControl == null || cam == null || _seaLevelRadius <= 0f)
+        DispatchUpdate(cam);
+        _presenting = true;
+    }
+
+    void UpdateAltitudeFade(Camera cam)
+    {
+        if (_precipSettings == null || _seaLevelRadius <= 0f)
         {
             _altitudeFadeAlpha = 1f;
             return;
         }
 
         float altitude = Vector3.Distance(cam.transform.position, _planetCenter) - _seaLevelRadius;
-        float maxAlt = _precipControl.LocalMaxCameraAltitude;
+        float maxAlt = _precipSettings.ParticleCeiling(_cloudSettings);
         _altitudeFadeAlpha = 1f - Mathf.Clamp01(Mathf.InverseLerp(maxAlt - AltitudeFadeBand, maxAlt, altitude));
     }
 
@@ -258,6 +286,7 @@
     float _uploadedStreakLength = float.NaN;
     float _uploadedVisibilityThreshold = float.NaN;
     float _uploadedDensityScale = float.NaN;
+    float _uploadedNearRadius = float.NaN;
     float _uploadedSeaRadius = float.NaN;
     Color _uploadedFadeColor = new Color(float.NaN, 0f, 0f, 0f);
     Vector3 _uploadedPlanetCenter = new Vector3(float.NaN, 0f, 0f);
@@ -279,20 +308,16 @@
             _runtimeMaterial.SetFloat(_rainVisibilityThresholdId, _uploadedVisibilityThreshold = VisibilityThreshold);
         if (DensityScale != _uploadedDensityScale)
             _runtimeMaterial.SetFloat(_rainDensityScaleId, _uploadedDensityScale = DensityScale);
+        if (CameraNearRadius != _uploadedNearRadius)
+            _runtimeMaterial.SetFloat(_rainNearRadiusId, _uploadedNearRadius = CameraNearRadius);
         if (_planetCenter != _uploadedPlanetCenter)
             _runtimeMaterial.SetVector(_rainPlanetCenterId, _uploadedPlanetCenter = _planetCenter);
         if (_seaLevelRadius != _uploadedSeaRadius)
             _runtimeMaterial.SetFloat(_rainSeaRadiusId, _uploadedSeaRadius = _seaLevelRadius);
     }
 
-    void DispatchUpdate()
-    {
-        Camera cam = Camera.main;
-        if (cam == null)
-            return;
-
-        if (_weatherProvider == null)
-            ServiceLocator.TryGet(out _weatherProvider);
+    void DispatchUpdate(Camera cam)
+    {
         Vector3 windDirection = _weatherProvider != null ? _weatherProvider.WindDirection : Vector3.right;
         float windSpeed = _weatherProvider != null ? _weatherProvider.WindSpeedMetersPerSecond : 0f;
 
@@ -313,9 +338,11 @@
         _updateCompute.SetInt(_frameSeedId, (int)(Time.frameCount * 2654435761u));
 
         _updateCompute.SetInt(_activeCountId, ParticleCount);
+        _updateCompute.SetInt(_resetParticlesId, _resetParticles ? 1 : 0);
         if (ParticleCount <= 0) return;
         int groups = (ParticleCount + 63) / 64;
         _updateCompute.Dispatch(_updateKernel, groups, 1, 1);
+        _resetParticles = false;
     }
 
     // --- Console commands -------------------------------------------------------------
--- a/Assets/Scripts/Planet/PrecipitationController.cs
+++ b/Assets/Scripts/Planet/PrecipitationController.cs
@@ -129,7 +129,7 @@
     static readonly int _weatherParticleProofId = Shader.PropertyToID(ShaderGlobalIds.WeatherParticleProof);
 
     public bool IsRenderingEnabled =>
-        TryResolveSettings()
+        isActiveAndEnabled && TryResolveSettings()
         && _seaLevelRadius > 0f
         && (IsDistantPrecipitationEnabled || IsLocalParticleSystemEnabled || _settings.RenderLocalParticles);
 
@@ -179,8 +179,7 @@
         float cameraAltitude = Vector3.Distance(camera.transform.position, _planetCenter) - _seaLevelRadius;
         // LocalMaxCameraAltitude is an absolute meter value that doesn't scale with planet size.
         // Cap at cloud-base altitude so particles never appear above the cloud layer regardless of scale.
-        float cloudBase = _cloudSettings?.BaseAltitude ?? 330f;
-        float maxAlt = Mathf.Min(_settings.LocalMaxCameraAltitude, cloudBase);
+        float maxAlt = _settings.ParticleCeiling(_cloudSettings);
         return cameraAltitude >= 0f && cameraAltitude <= maxAlt;
     }
 
@@ -279,9 +278,8 @@
         Shader.SetGlobalInt(_weatherParticlesEnabledId, IsLocalParticleSystemEnabled ? 1 : 0);
         if (!IsRenderingEnabled) return;
 
-        float cloudBaseAltitude = _cloudSettings != null ? _cloudSettings.BaseAltitude : 330f;
         float bottomRadius = _seaLevelRadius + _settings.BottomAltitude;
-        float topRadius = _seaLevelRadius + Mathf.Max(_settings.BottomAltitude + 1f, cloudBaseAltitude + _settings.CloudBaseOverlap);
+        float topRadius = _seaLevelRadius + _settings.ColumnTopAltitude(_cloudSettings);
 
         Shader.SetGlobalVector(_precipitationPlanetCenterId, _planetCenter);
         Shader.SetGlobalVector(_precipitationRadiiId, new Vector4(bottomRadius, topRadius, _settings.MaxDistance, _seaLevelRadius));
@@ -308,10 +306,9 @@
             Mathf.Max(_settings.DebugDotMaxRadius, _settings.DebugDotMinRadius + 0.01f),
             _settings.DebugDotOpacity,
             0f));
-        float cloudBase = _cloudSettings?.BaseAltitude ?? 330f;
         Shader.SetGlobalVector(_weatherParticleCommonId, new Vector4(
             _settings.LocalParticleRadius,
-            Mathf.Min(_settings.LocalMaxCameraAltitude, cloudBase),
+            _settings.ParticleCeiling(_cloudSettings),
             _settings.LocalParticleVerticalRange,
             _settings.DustTurbulence));
         Shader.SetGlobalVector(_weatherParticleCountsId, new Vector4(
--- a/Assets/Scripts/Planet/PrecipitationDto.cs
+++ b/Assets/Scripts/Planet/PrecipitationDto.cs
@@ -48,6 +48,12 @@
     float DebugDotMaxRadius,
     float DebugDotOpacity)
 {
+    public float ColumnTopAltitude(CloudDto clouds) =>
+        Mathf.Max(BottomAltitude + 1f, (clouds?.BaseAltitude ?? 330f) + CloudBaseOverlap);
+
+    public float ParticleCeiling(CloudDto clouds) =>
+        Mathf.Max(0f, Mathf.Min(LocalMaxCameraAltitude, clouds?.BaseAltitude ?? 330f));
+
     public static PrecipitationDto From(PrecipitationController c) => new(
         c.RenderPrecipitation,
         c.Intensity,
--- a/Assets/Scripts/Planet/WeatherLightningController.cs
+++ b/Assets/Scripts/Planet/WeatherLightningController.cs
@@ -33,6 +33,7 @@
 
     IWeatherProvider _weather;
     ISeedProvider _seeds;
+    WeatherThunderPlayback _thunder;
     int _systemSeed;
     int _strikeIndex;
     Rng _rng = Rng.CreateFromIndex(0);
@@ -59,6 +60,7 @@
 
     void OnEnable()
     {
+        _thunder = new WeatherThunderPlayback();
         EventBus<PlanetGeneratedEvent>.Listen(OnPlanetGenerated);
         ScheduleNextStrike(1f, 3f);
         ClearLightning();
@@ -66,6 +68,8 @@
 
     void OnDisable()
     {
+        _thunder?.Dispose();
+        _thunder = null;
         EventBus<PlanetGeneratedEvent>.Unlisten(OnPlanetGenerated);
         ClearLightning();
     }
@@ -90,6 +94,7 @@
     {
         _planetCenter = evt.PlanetCenter;
         _hasPlanet = true;
+        _thunder?.Reset(_planetCenter, evt.SeaLevelRadius > 0f ? evt.SeaLevelRadius : evt.PlanetRadius);
         RefreshSeed();
     }
 
@@ -144,6 +149,11 @@
             ? _strikePower * Mathf.Lerp(0.25f, 0.55f, _rng.NextFloat())
             : 0f;
         float strikeRadius = (stormPosition - _planetCenter).magnitude;
+        if (SettingsProvider.IsRegistered<CloudDto>())
+        {
+            CloudDto clouds = SettingsProvider.GetSettings<CloudDto>();
+            strikeRadius += clouds.BaseAltitude + clouds.LayerThickness * 0.5f;
+        }
         Vector3 strikePosition = _planetCenter + _strikeDirection * strikeRadius;
         EventBus<WeatherLightningEvent>.Raise(new WeatherLightningEvent(
             strikePosition,
--- /dev/null
+++ b/Assets/Scripts/Planet/WeatherThunderPlayback.cs
@@ -0,0 +1,99 @@
+using System;
+using UnityEngine;
+
+sealed class WeatherThunderPlayback : IDisposable
+{
+    const int VoiceCount = 4;
+    const float SoundSpeed = 343f;
+    const float AudibleDistance = 10000f;
+    readonly AudioSource[] _voices = new AudioSource[VoiceCount];
+    readonly double[] _busyUntil = new double[VoiceCount];
+    readonly AudioClip[] _clips;
+    AudioListener _listener;
+    Vector3 _planetCenter;
+    float _planetRadius;
+    int _clipIndex;
+
+    public WeatherThunderPlayback()
+    {
+        _clips = Resources.LoadAll<AudioClip>("Weather/Thunder");
+        Array.Sort(_clips, (a, b) => string.CompareOrdinal(a.name, b.name));
+        EventBus<WeatherLightningEvent>.Listen(OnLightning);
+    }
+
+    public void Reset(Vector3 center, float radius)
+    {
+        _planetCenter = center;
+        _planetRadius = radius;
+        _clipIndex = 0;
+        for (int i = 0; i < VoiceCount; i++)
+        {
+            if (_voices[i] != null) _voices[i].Stop();
+            _busyUntil[i] = 0;
+        }
+        _listener = UnityEngine.Object.FindAnyObjectByType<AudioListener>();
+    }
+
+    void OnLightning(WeatherLightningEvent evt)
+    {
+        if (_clips.Length == 0 || !Finite(evt.WorldPosition) || !float.IsFinite(evt.Intensity))
+            return;
+        if (_listener == null || !_listener.isActiveAndEnabled)
+            _listener = UnityEngine.Object.FindAnyObjectByType<AudioListener>();
+        if (_listener == null || !_listener.isActiveAndEnabled)
+            return;
+
+        Vector3 listener = _listener.transform.position;
+        Vector3 segment = evt.WorldPosition - listener;
+        float distance = segment.magnitude;
+        if (distance > AudibleDistance || evt.Intensity <= 0f)
+            return;
+        // A straight sound path through the planet must not sound like a nearby storm.
+        float t = Mathf.Clamp01(Vector3.Dot(_planetCenter - listener, segment)
+            / Mathf.Max(segment.sqrMagnitude, 0.0001f));
+        if (_planetRadius > 0f && (listener + segment * t - _planetCenter).sqrMagnitude
+            < _planetRadius * _planetRadius)
+            return;
+
+        double now = AudioSettings.dspTime;
+        for (int i = 0; i < VoiceCount; i++)
+        {
+            if (_busyUntil[i] > now) continue;
+            AudioSource voice = _voices[i];
+            if (voice == null)
+            {
+                var host = new GameObject("Weather thunder") { hideFlags = HideFlags.HideAndDontSave };
+                voice = host.AddComponent<AudioSource>();
+                voice.playOnAwake = false;
+                voice.spatialBlend = 1f;
+                voice.dopplerLevel = 0f;
+                voice.rolloffMode = AudioRolloffMode.Logarithmic;
+                voice.minDistance = 100f;
+                voice.maxDistance = AudibleDistance;
+                _voices[i] = voice;
+            }
+            AudioClip clip = _clips[_clipIndex++ % _clips.Length];
+            voice.transform.position = evt.WorldPosition;
+            voice.clip = clip;
+            voice.volume = Mathf.Clamp01(evt.Intensity);
+            double arrival = now + distance / SoundSpeed;
+            voice.PlayScheduled(arrival);
+            _busyUntil[i] = arrival + clip.length;
+            return;
+        }
+    }
+
+    static bool Finite(Vector3 point) =>
+        float.IsFinite(point.x) && float.IsFinite(point.y) && float.IsFinite(point.z);
+
+    public void Dispose()
+    {
+        EventBus<WeatherLightningEvent>.Unlisten(OnLightning);
+        foreach (AudioSource voice in _voices)
+        {
+            if (voice == null) continue;
+            voice.Stop();
+            UnityEngine.Object.Destroy(voice.gameObject);
+        }
+    }
+}
--- /dev/null
+++ b/Assets/Scripts/Planet/WeatherThunderPlayback.cs.meta
@@ -0,0 +1,11 @@
+fileFormatVersion: 2
+guid: 42c9272282c94c3895034f7247da7f59
+MonoImporter:
+  externalObjects: {}
+  serializedVersion: 2
+  defaultReferences: []
+  executionOrder: 0
+  icon: {instanceID: 0}
+  userData: 
+  assetBundleName: 
+  assetBundleVariant: 

# Shared creature audio

`Grazing.ogg` is the one clip used by more than one species, which is why it sits
here instead of in a species folder.

Provenance is not recorded. The file carries no `AssetOrigin` metadata, it is
untracked in git, and no design document names it. Re-establish where it came from
before production use.

# Crafting recipes

Both assets here are ours. Nothing came from a pack.

`Cook meat.asset` and `Grind wildflowers.asset` are the two recipes the crafting
transaction system resolves. Each names its inputs, its output, and the interaction
definition that performs it.

They sit beside `../Definitions` because a recipe is meaningless without the
definition that runs it.

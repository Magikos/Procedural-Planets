---
name: project-code-refactor-arc
description: "Codebase-wide refactor arc on branch `code-refactor`. Audit-first per [[feedback-audit-workflow]]. Biome+Planet DTO sub-arc shipped 2026-06-11; broader hotspot work continues."
metadata:
  node_type: memory
  type: project
  originSessionId: 97829702-a6c8-47a8-a3db-f18c9ac1f8af
---

Started 2026-06-10 after consolidating phase8-spawning-foundation → main. New branch `code-refactor` is the working branch for this arc.

**Why:** Bryan wants to clean up accumulated drift before the next feature push and codify "new rules going forward" into CLAUDE.md so future work stays consistent.

**How to apply:**
- Treat this as the active project focus until Bryan signals otherwise. Supersedes biome-overhaul as the current-focus arc but the biome work is paused, not cancelled.
- Follow [[feedback-audit-workflow]] strictly: audit findings first, Bryan reviews, only THEN fix. Do not start fixing during the audit phase.
- Scope confirmed 2026-06-10: all four motivations (architectural consistency, cross-coupling reduction, perf/allocations, style/readability) and all four hotspots (Planet.cs/generation, weather/precipitation stack, grass/vegetation, debug/console/services).
- New rules will be collaborated on live (not pre-drafted by Claude).
- Audit deliverables go under `docs/audits/2026-06-code-refactor/` (one doc per hotspot + a top-level summary).

**Biome+Planet DTO sub-arc shipped 2026-06-11 (7 slices on `code-refactor`):**
- BiomeRegistry threshold cull → BiomeConstants
- BiomeSettings knob cull (23 → 14 fields) → BiomeConstants
- BiomeDefinitionDto
- BiomeRegistryDto + BiomeDto + consumer migration (ColorGenerator, TemperatureProvider, BiomeSurfaceTextureArrays, Planet.cs); IBiomeRegistry deleted
- ClimateCommands rewrite: all 17 commands route through `SettingsProvider.Update<BiomeDto>(dto with { ... })`; ClimateCurves helper extracted
- PlanetDto + 31-site Planet.cs migration; PlanetSettings stripped of methods; PlanetSettingsAsset getter restricted to `#if UNITY_EDITOR`

After this arc, the [[feedback-settings-dto-pattern]] is fully realized for all hub SOs (Atmosphere, Cloud, Biome, Planet). The 7-step recipe (cull → DTO → consumer migration → drop SO runtime methods → register-if-not-registered boot → commands-via-Update) is the established template for new Settings SOs.

**Progress as of 2026-06-12 (on `code-refactor`):**
- Perf-maintainability plan slices 1–4 done. Slice 4 = the `ChunkedSurfaceProvider` restructure (~2,064 → 546 lines) into acyclic collaborators: `BiomeAtlasService`, `ChunkSurfaceGenerator`, `ChunkMeshCache`, `ChunkVisibilitySelector` (+ stateless `GrassSurfaceAtlasBuilder` / `ChunkSurfaceQueries`). Worked plan: docs/design/2026-06-12-chunked-surface-provider-restructure.md. Decomposition wiring rule: [[feedback-subsystem-decomposition-wiring]]. Also fixed a render-handle pool bug that starved visible chunks (black holes).
- T1 Settings-DTO tail closed: PLANET-14 (clone PlanetMaterial, stop dirtying the SO asset) and PLANET-15 (ShapeSettings → plain class, kills per-gen throwaway-SO leak). Confirmed the read-migration audit (2026-06-10) was already stale — all five named consumers read DTOs.
- T5 dead-code subtraction DONE (commit e5cf37f, ~1,711 lines): deleted GpuChunkSurfaceProvider (never instantiated) and the whole GrassMidField experiment (controller/shader/compute + Planet.cs wiring + GrassRenderLayer.Mid + stats types + debug blocks), plus the dead SmokeRenderer flag. Most other T5 items were already gone (self-tests, CombinedFaceMesh, #if false block, GrassPlacementClimateBinding, WEATHER-4 procedural-rain pass/DistantRain fields) — the 2026-06-10 audit is stale on much of T5.
- T3 boot-path DONE (commit 0c1412f): the real remaining violation was `CubeFaceTopology.RunSelfTestAtStartup` (a `[RuntimeInitializeOnLoadMethod]` self-test the audit missed) — deleted the whole `#if UNITY_EDITOR || DEVELOPMENT_BUILD` block. `EventBusProcessor` was already lazy/self-creating (audit CORE-1 stale). `LoadingManager` is now the ONLY `RuntimeInitializeOnLoadMethod` in the codebase — CLAUDE.md boot-path rule fully satisfied.
- T7 debug-module hygiene DONE (commit 6118269): extracted AtmosphereDebugModule + ScaleReferenceDebugModule out of GrassDebugModule (CORE-8); Biome/Terrain modules already existed (CORE-3 already done). Debug-capture pattern = IDebugModule + IDebugCaptureMetadataProvider self-registering with DebugRegistry; composition root is DebugCaptureController. One optional follow-up left: WaterDebugModule still owns an AppendWeatherMetadata block (defensible — weather drives water FX — not a clear rule violation).
- Slice 6 god-class splits STARTED on Planet.cs (one extraction at a time, validate-then-commit). First out (commit 76c1cd9, validated): PlanetWaterSurface — water GameObject + cloned ocean material + async water build (GenerateAsync/BuildWaterMeshAsync/CreateWaterMaterial/UpdateWaterMaterial) + 34 water shader IDs + colors/constants. Pure relocation; Ocean.shader/caustics untouched. Planet.cs 1,043 -> 775. Second out (commit 594d637, validated): PlanetGrassCoordinator — both grass controllers + enable flags + altitude near-field activation + grass-far-overlay terrain-material block + IGrassRuntimeControl impl. Planet stays the registered IGrassRuntimeControl and forwards the 3 methods; coordinator takes Planet as IPlanetSurfaceSampler for the altitude radius query; reads PlanetDto live from SettingsProvider. Planet.cs 775 -> 497. Third out (commit 4643d64, validated): PlanetTerrainMaterial — runtime-clone lifecycle (EnsureRuntime/Dispose), vertex-color shader swap, grass-overlay hook, coast/slope/snow surface overrides + SetMaterialAndGlobal helpers + IDs. Takes the grass coordinator in Configure to preserve call order; reads PlanetDto/BiomeDto live. CLAUDE.md clone-on-first-use invariant intact. Planet.cs 497 -> 406. **Planet.cs decomposition COMPLETE** (1,043 -> 406): now a thin orchestrator over PlanetWaterSurface + PlanetGrassCoordinator + PlanetTerrainMaterial (plus the existing ShapeGenerator/ColorGenerator/surface-provider collaborators). All three new collaborators are plain classes in Assets/Scripts/Planet/, constructed in Awake, disposed in OnDestroy.
- Slice 6 fourth cut (commit 21788e7, validated): WeatherDiagnostics extracted from WeatherManager — all observability (F9 JSON dump + formatter, OnGUI overlay, readback aggregate stats, evolution telemetry + ~22 fields). WeatherManager keeps simulation/evolution/wind-globals/IWeatherProvider-sampling/query-cache/console; drives diagnostics via Tick/RecordEvolutionDispatch/OnQueryCacheFaceData/Reset/DrawOverlay/OnDiagnosticsRequested. Collaborator reads core live via 6 narrow internal accessors (Grid, PrecipitationDebugControl, CalculatePrecipitation, QueryCacheFaceCount/LastFace/Error); inspector toggles stay on the MB. WeatherManager 871 -> 451. **Established a 5th collaborator pattern for MonoBehaviour god-classes: plain-class debug/observability companion that the MB owns + feeds, reading core state through internal accessors.**
- Slice 6 fifth cut (commit 9e26cb2, validated): CameraTeleportStore extracted from FreeCameraController — the whole teleport domain (saved-location state+cap, built-ins, name rules, lookup, JSON/PlayerPrefs persistence, F10 pose import, data types) INCLUDING the 4 camera.* teleport console commands + ICameraTeleportRegistry. FreeCameraController 859 -> 415. **NEW PATTERN unlocked: a plain (non-MonoBehaviour) class can host console commands via MonoTargetType.Registry + ConsoleRegistry.RegisterInstance(this) in its ctor; multiple types can share one [CommandPrefix] since discovery scans ALL types and only duplicate *aliases* collide.** Camera-pose ops the store needs are delegated back through a new `ICameraTeleportTarget : ICameraRigContext` (CaptureLocation/TryApply) that the controller implements; CameraTeleportLocation promoted to a top-level type to cross that boundary. Console registration system: ConsoleRegistry.Scan() reflects [CommandPrefix]/[ConsoleCommand] over all assemblies; instance resolution at exec time via MonoTargetType (Static/Single=FindAnyObjectByType/Registry=GetInstance).
- Slice 6 sixth+seventh cuts (GrassPlacementController, two seams, both validated): seam A (commit 70c3b36) promoted the nested per-chunk GPU wrapper to its own file GrassChunkRuntime.cs (GrassStatsCount -> GrassChunkRuntime.StatsCount); seam B (commit 167ed62) grouped the ~40 _last* debug counters + 16 Stat* buffer-index consts + AccumulateRuntimeStats into GrassPlacementStats.cs (controller writes _stats.*, GetGrassDebugStats stays the registered IGrassDebugStatsProvider and composes telemetry+config). Reset/ownership semantics preserved exactly. GrassPlacementController 780 -> 494.
- Slice 6 eighth+ninth cuts (WaterDebugModule, two seams, both validated): seam 1 (commit a5e29cc) moved the declarative mode/capture-set catalog to static WaterDebugRegistration.cs (Register() delegates); seam 2 (commit 69bae75) moved the CPU mesh analysis (vertex-color histogram + camera sample + topology/radius integrity checks) to WaterMeshAnalysis.cs, with WaterDebugStats/MeshIntegrityStats promoted to top-level. Module keeps debug-surface plumbing + F10/overlay text. WaterDebugModule 769 -> 243. No Ocean.shader/caustics code touched.
- **T6 shader-globals stance DECIDED + DONE (2026-06-13): full centralization.** Bryan chose: every shader-*global* name lives in `ShaderGlobalIds`, now a `partial` class split one file per domain (ShaderGlobalIds.Core/.Atmosphere/.Water/.Cloud/.Grass/.Precipitation/.Terrain/.Biome/.Celestial.cs, in Core/Services/). CLAUDE.md "Shader globals" section rewritten to this rule. **Scope clarification that mattered:** the "387 PropertyToID sites" headline conflated globals with material/compute property IDs — only ~150 are true globals (names passed to Shader.SetGlobal*/GetGlobal* or RenderGraph UseGlobalTexture/SetGlobalTextureAfterPass). Material-property (`mat.SetFloat`) and compute-shader (`compute.SetBuffer`) property names are shader-scoped, NOT globals, and stay module-local. Migrated domain-by-domain, validate-in-Unity + commit each: 6a1c791 (infra+Atmosphere), 5347d76 (Water), ed205d5 (Cloud/Weather), 4400f49 (Grass), 091b9c5 (Precipitation), 7b7f62e (Terrain/Biome), 9fa777e (Celestial). Modules still cache their own `static readonly int _xId = Shader.PropertyToID(ShaderGlobalIds.X)` locally — hub owns names, not cached IDs. Shared names that were both a global and a compute/material literal (e.g. _WindDirection, _WindSpeedMps, _BiomeGrassParams, _BiomeGrassParamCount, _GrassSurfaceAtlasResolution) were pointed at the same const everywhere to close divergence. Final sweep verified: empty intersection of raw-string-PropertyToID field names vs global-usage field names; zero inline raw-string globals; 151 unique const values. Pure name substitution — PropertyToID returns identical ints, runtime unchanged. (Aside Bryan noticed during grass validation: grass interaction is slightly jerky on first interaction — pre-existing, NOT caused by T6; possible future look: interactor buffer/velocity state not warm on first registered frame.)
- **Slice 5 item 1 (timing counters) DONE (2026-06-13, commit f16d296).** FrameTimingCounters (CPU per-section via a readonly-struct Stopwatch scope `using (FrameTimingCounters.Measure(section))`, lazy frame-roll off Time.frameCount + whole-frame CPU/GPU from FrameTimingManager) + FrameTimingModule (overlay + F10 sidecar), mirroring MemoryDebugModule. Instrumented the real per-frame CPU loops: SurfaceVisibility (ChunkedSurfaceProvider.Tick), Water (WaterWakeController, body moved to PublishWakeSources), Clouds (CloudController.Update), NearGrass + ChunkGrass (PlanetGrassCoordinator.Tick). **Finding: terrain has NO separate per-frame CPU loop** — its LOD/visibility/mesh-activation CPU is the surface tick; render cost is GPU (whole-frame). So no standalone Terrain section (dropped rather than ship a zero). First real numbers (orbit): whole-frame CPU 14.6ms / GPU 17.0ms; chunk grass 1.17ms is the heaviest per-section CPU. Also reorganized the debug overlay into F6 (Camera + Performance always-on) vs F9 (Controls/State/modules, scroll) tiers, and renamed the misleading "water details" toggle → "detailed debug" across event/command-type/field/console-command/F9-hint. Items 2 (skip-unchanged work), 3 (grass cards = visual), 4 (pooled grass buffers) remain — gated on this evidence.
- **Commit f16d296 was a COMBINED commit** (Bryan's call, "commit them all together"): my Slice 5 + overlay work bundled with a parallel **world-lifecycle / SettingsService refactor authored by Codex** (a separate agent working the SAME branch/tree concurrently): IWorldContext/IWorldServiceRegistrar/IWorldSettingsRegistrar wiring, SettingsService, biome .asset updates, Shape.asset removal (PLANET-15), 2026-06-13-world-lifecycle.md. **Coordination caveat going forward: Codex is live in the grass/clouds/settings/bootstrap hot files — Slice 5 items 2–4 and the grass-card item all touch those same files (high collision risk). Prefer isolated tasks (e.g. ConsoleController split) unless Bryan confirms Codex is clear of a given area.**
- **ConsoleController split STARTED (2026-06-13, isolated from Codex). Two cuts, both validated:** (1) commit 1c71d90 — ConsoleInputLineFormatter (input-line span/syntax-highlight builder: spinner prefix, prompt, syntax-highlighted typed text + cursor, ghost completion, param-slot hints; keeps reusable span lists, allocation-free). Controller computes ghostActive + spinner dots and delegates; ShouldShowGhost stays on controller (PopupVisible needs it). (2) commit 1d4e11e — ConsoleAsyncRunner (command execution + single-in-flight async: owned CTS, animated "running..." line, abandon/cancel, ObservePending/AwaitGeneric). Ctor (_scrollback, this as IConsoleService, ShowConfirm delegate); controller delegates SubmitInputLine→TryRunInteractive (returns false=rejected so typed line preserved), RunCommand→RunProgrammatic, BeginAsync/Abandon/RequestCancel pass-throughs, Update→Tick, OnDestroy→Shutdown, GetDiagnostics via accessors. CTS re-entrancy preserved; `this != null` MB-destruction guard became a _shutdown flag. (3) commit 6b97748 — ConsoleInputController (input-editing state machine: text buffer, intellisense suggestions + popup nav, history, held-key repeat, scrollback view offset, confirm mode). Controller drives via Tick(_input) when open, reads render accessors (Text/CursorPos/Suggestions/GhostActive/IsConfirmMode/...). runner↔input mutual ref resolves via lazy closure in Awake. ShowConfirm/confirm moved to input controller; Confirm()/cancel-prompt route there; Open/Close→NotifyOpened/NotifyClosed (history load/save + text hook). **ConsoleController split COMPLETE: 1083 → 265** (ConsoleController 265 lifecycle+IConsoleService+render-orchestration, ConsoleInputController 491, ConsoleAsyncRunner 321, ConsoleInputLineFormatter 148). Note WasPerformedThisFrame() is a UnityEngine.InputSystem extension on InputAction — controller keeps that using for OpenConsole/CloseConsole.
- **Codex footprint is spreading:** as of 2026-06-13 Codex has expanded FrameTimingModule.cs (added FrameTimingStats rolling-window/avg/p95/uninstrumented-CPU/Reset) and added IChunkVisibilitySource to ChunkedSurfaceProvider — so it's now also in the perf-timing + surface files, not just grass/clouds/settings/bootstrap. Keep checking `git status` before staging and stage ONLY my own files per commit.
- **Slice 5 CLOSED (2026-06-13, commit a37390b, bundled with a Codex tree per Bryan's "bundle like last time").** Item 1 (counters) shipped earlier (f16d296). Item 4: GrassBladeBufferPool — per-chunk blade GraphicsBuffer reused on page in/out (only the blade buffer; args/stats stay per-runtime since they carry AsyncGPUReadback). Kills driver/GC churn on fly-through, not steady memory. Item 2 evaluated + SKIPPED (GPU-bound; re-dispatch already gated on 25m; residency can't gate on position alone since rotation changes the frustum). Item 3 (distant card LOD) DEFERRED by Bryan. **Grass-line fix (the real ask):** the visible line was painted terrain grass reading lighter than the darker 3D tufts (same hue via shared GradeGrassTint, different value). Added `_GrassSurfaceBrightness` material param to PlanetVertexColor.shader (multiplies the painted grass in ApplyGrassSurfaceAlbedo) + live `grass.*` console knobs on PlanetGrassCoordinator (plain-class host: [CommandPrefix("grass")] + ConsoleRegistry.RegisterInstance(this) in ctor, MonoTargetType.Registry). Bryan tuned live → baked surface-brightness=0.4, strength stays 1.0. Removed knobs that proved ineffective: overlay-start/end only fed a debug viz (farWeight/midWeight/nearWeight used solely at the debug return), noise/fiber imperceptible. Kept surface-brightness + overlay-strength + overlay-status. **Lesson: the far-overlay's distance weights don't drive the surface albedo — ApplyGrassSurfaceAlbedo is distance-independent; only envCoverage*strength + the grading/brightness matter for the ground look.**
- Remaining: DebugCaptureController split (~700, but Codex now also edits it — added WorldReadyEvent/OnWorldReady, so collision risk rising); audit backlog still needs Bryan's keep/defer/fix marks. **All originally-flagged god-classes now under ~500 lines; T5/T3/T7/T6 and the ConsoleController split all closed.**
- **2026-06-15 session: ALL audit backlog items closed.** WEATHER-2 (WeatherEvolutionScheduler + WeatherQueryCache extracted from WeatherManager); WEATHER-6/8/9 (RainParticleController: IWeatherProvider wind, Destroy, commands on MB); WEATHER-10 (DumpAtmosphereDiagnostics via DebugInputRelay); WEATHER-12 (render features → ServiceLocator.TryGet); WEATHER-13 (_seaLevelRadiusId rename); WEATHER-14 (bool _windDirty); WEATHER-15 (WeatherLightning.hlsl extract); WEATHER-16 (WeatherCubeFace.hlsl extract); WEATHER-18 (TryFindStrongestStorm deleted, fused into CalculateStats); PLANET-9 (GetVisibleChunksSnapshot output-list); CORE-10 (CachedSingleTarget on CommandData); CORE-11 (ConsoleRegistry assembly prefix filter); CORE-13 (IMemoryReporter pull model replaces MemoryDebugCounters); GRASS-8 (altitude consts to IGrassQualitySettings). **Arc COMPLETE.**
- **2026-06-14 session: TintDryShift/TintLushShift wired end-to-end (GRASS-1 tail).** GPU struct grew 48→80 bytes (Shape/Placement/Tint/TintDry/TintLush as 5 Vector4s). GrassParamsStride updated in BiomeSurfaceTextureArrays. Both compute shaders (BiomeGrassPlace, GrassNearFieldPlace) extended BiomeGrassParams struct + BlendGrassParams + LerpGrassParams + SampleClimateMoisture(_ClimateMap Texture2DArray, G=moisture01). GrassPlacementController + GrassNearFieldController bind climate map before dispatch via GetClimateMap() (falls back to lazy 1×1×6 neutral Texture2DArray if global not yet set). GRASS-5 (Warning→Info for feature-disabled paths) fixed in both controllers. Default authoring values are Color.white = no shift, so existing biomes unchanged until designers set non-white values.
- **Session tally (2026-06-12/13): 7 validated Slice-6 god-class cuts on `code-refactor`: PlanetWaterSurface, PlanetGrassCoordinator, PlanetTerrainMaterial (Planet 1,043 -> 406), WeatherDiagnostics (Weather 871 -> 451), CameraTeleportStore (FreeCamera 859 -> 415), GrassChunkRuntime + GrassPlacementStats (Grass 780 -> 494). Each: pure-relocation-style extraction, validate-in-Unity, commit. Established collaborator patterns: owned plain-class sub-services constructed in Awake/disposed in OnDestroy; debug/observability companions reading core via narrow internal accessors; plain-class console-command providers via MonoTargetType.Registry under a shared prefix.**

**State of branches as of 2026-06-10:**
- Cleaned up: phase3-foundation, phase4-biomes, code-quality-fixes, agents/amazonq-uncommitted-git-check, copilot/worktree-2026-05-25T00-08-13, phase8-spawning-foundation, origin/park_1 — all deleted (work preserved in main).
- One cherry-pick onto main: `a5e068b` (DebugCaptureController/FreeCameraController TryGet<> safety from the copilot branch). The shader half of that fix was obsoleted by the symbolic-debug-mode refactor.
- Live branches: local `main` and `code-refactor` (current); remote `origin/main` only.

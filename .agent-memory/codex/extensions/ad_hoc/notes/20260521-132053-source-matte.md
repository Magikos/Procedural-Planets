# ProceduralPlanets Source Matte Pass

[ad-hoc note] Bryan's F10 sets around `20260521-131038`, `20260521-131133`, `20260521-131146`, and `20260521-131201` confirmed the shoreline-like artifact is terrain source color. In above-water sets, `TerrainSourcePink` turns the offending contour hot pink while `FoamPink` does not mark it.

[ad-hoc note] The active fix belongs in `WaterVolume.shader`, not `Ocean.shader` foam. The line remains most visible in `Off` and `VolumeOnly`, while `WaterOff` and `SurfaceOnly` expose the underlying terrain/shore source directly.

[ad-hoc note] `WaterVolume.shader` now strengthens `sourcePathOcclusion`, raises source-occlusion contribution, increases density for source-occluded pixels, clamps source transmittance much lower, uses `sourceMatte` for full volume blend, and pushes deep extinction higher for source-color pixels.

[ad-hoc note] Added `brightSourceBleed` based on source luminance so thin white/yellow shoreline highlights are matted toward deep water color without globally darkening all open water.

[ad-hoc note] Next F10 review should compare `Off`, `VolumeOnly`, `WaterOff`, `VolumeOcclusion`, `TerrainSourcePink`, `FoamPink`, and `VolumeSphere`. Expected result: the hot-pink/white shoreline strip should be gone or much more blue/dark in `Off` and `VolumeOnly`.

[ad-hoc note] Verification: `dotnet build ProceduralPlanets.Planet.csproj` passed and no trailing whitespace was found in `WaterVolume.shader`. Unity still needs to reimport/compile `WaterVolume.shader`.
